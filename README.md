# Express × TypeScript

Docker Build:

```bash
# show logs
docker-compose up

# background
docker-compose up -d
```

Express Build:

```bash
# develop (with watch)
yarn dev

# production (output ./.build directory)
yarn build
```
